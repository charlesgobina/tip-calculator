import { ReactComponent as TipLogo } from '../../assets/images/logo.svg';

const Logo = () => (
  <div style={{ textAlign: 'center', padding: '40px 0' }}>
    <TipLogo />
  </div>
);

export default Logo;
